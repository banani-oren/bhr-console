# Transactions — `/transactions`

**Captured:** Round 2 autonomous run (clean DB).

**Header:** `עסקאות` right-aligned. Top-right actions: `ייבוא` (upload) + `הוסף עסקה` (purple plus button).

**6 filter dropdowns** in order (right-to-left): `חודש כניסה | חודש סגירה | סוג שירות | ליד שירות | סטטוס חיוב | שנת סגירה`, all defaulting to `all`.

**Empty state:** `לא נמצאו עסקאות` — thead not rendered when no rows (normal behavior).

**Sidebar:** `עסקאות` highlighted active.

**Console:** clean.
