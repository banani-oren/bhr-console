# Hours Log — `/hours`

**Captured:** Round 2, clean DB.

**Header:** clock icon + `יומן שעות` right-aligned.
**Month/year selector defaults** to current month: `חודש=4`, `שנה=2026`, badge `אפריל 2026` (today is 2026-04-18).

**Empty state:** `אין נתוני שעות לחודש זה` — no tabs yet (tabs derive from hours_log rows, which are zero in clean DB).

**Sidebar:** `יומן שעות` highlighted active.

**Console:** clean.
